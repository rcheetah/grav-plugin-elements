title: Container
extends@: default

form:
  fields:
    tabs:
      fields:

        # Extending Tab Content 
        content:
          type: tab
          title: Spalten
          fields:

            content: 
              unset@: true
            
            title:
              label: "TEST"

            header.media_order:
              unset@: true

            page_fieldset_title:
              unset@: true

            module_note:
              type: display
              size: long
              label: "**HINWEIS**"
              style: vertical
              markdown: true
              content: "Füge einfach Module unterhalb dieser Seite hinzu. Die Elemente werden dann untereinander dargestellt. <br> ACHTUNG: Der Titel dieser Seite ist nur intern, und wird nicht ausgegeben!"

            modular_title:
              type: spacer
              title: PLUGIN_ADMIN.MODULAR_SETUP

            header.content.items:
              type: text
              label: PLUGIN_ADMIN.ITEMS
              default: '@self.modular'
              size: medium

            header.content.order.by:
              type: text
              label: PLUGIN_ADMIN.ORDER_BY
              placeholder: date
              size: small

            header.content.order.dir:
              type: text
              label: PLUGIN_ADMIN.ORDER
              help: '"desc" or "asc" are valid values'
              placeholder: desc
              size: small