title: Container
extends@: modular

form:
  fields:
    tabs:
      fields:

        # Extending Tab Content 
        content:
          title: Container
          
          fields:

            content:
              unset@: true

            element.spacer.main:
              type: spacer
              title: PLUGIN_ELEMENTS.ELEMENTS.CONTAINER.NAME
              ordering@: header.title

            element.container.info:
              type: display
              size: long
              style: vertical
              markdown: true
              label: PLUGIN_ELEMENTS.ELEMENTS.CONTAINER.NOTE.TITLE
              content: PLUGIN_ELEMENTS.ELEMENTS.CONTAINER.NOTE.CONTENT
              ordering@: element.spacer.main


            header.media_order:
              ordering@: 1000


        element.layouttab:
          fields:  
            element.layouttab.basics:
              fields:
                header.element.contentlayout:           
                  default: default
                  disabled: false
                  help: unset@
                  classes: unset@
       
        # doesnt work
        advanced:
          fields:
            columns:
              fields:
                column2:
                  fields:
                    ordering:
                      default: 1