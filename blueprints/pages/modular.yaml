extends@: modular

form:
  fields:
    tabs:
      fields:
        element.layouttab:
          fields:
            element.layouttab.basics:
              fields:
                header.element.contentlayout:           
                  default: full
                  disabled: true
                  help: PLUGIN_ELEMENTS.TAB.LAYOUT_MODULARNOTE
                  classes: "field--locked"