extends@:
  - default


form:
  fields:
    tabs:
      fields:
        content:
          fields:

            # Hide page header
            header.element.pagehead.image:
              unset@: true
