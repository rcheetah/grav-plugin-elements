extends@: default

form:
  fields:
    tabs:
      fields:
        content:
          fields:
            header.element.module.section:
              type: section
              title: MODULE
              underline: true
              fields:
                header.element.module.bgcolor:
                  type: select
                  label: PLUGIN_ELEMENTS.ELEMENT_TAB_BGCOL
                  toggleable: true
                  default: default
                  config-options@: plugins.elements.layoutoptions.colors